fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
bcrypt>=4.0.1
python-multipart==0.0.20
sse-starlette==2.2.1
httpx==0.28.1
python-dotenv==1.0.1

# 阿里云 PNVS 短信认证（生产环境 SMS_DEV_MODE=0 时使用）
alibabacloud_dypnsapi20170525>=2.0.0
alibabacloud_tea_openapi>=0.3.12
alibabacloud_tea_util>=0.3.13

# RAG / 向量库（langchain 仅用其文本切分器；chromadb 单独装，见 install.bat；
# embedding 用 chromadb 默认 ONNX 函数，无需 sentence-transformers）
langchain-text-splitters==0.3.4

# 文档解析
pdfplumber==0.11.4
python-pptx==1.0.2
python-docx==1.1.2
olefile==0.47
pywin32>=306; sys_platform == "win32"

# 视频（whisper 为可选依赖，见 requirements-video.txt；未装时视频转写不可用，其余功能正常）
ffmpeg-python==0.2.0

# 知识推送
apscheduler==3.10.4
feedparser==6.0.11
